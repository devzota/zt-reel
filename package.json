{
  "name": "zt-reel",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start:api": "npm run start:dev --workspace=apps/api",
    "start:web": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "ts-morph": "^28.0.0"
  }
}
